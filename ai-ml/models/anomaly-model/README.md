# AI012 Anomaly Detection

Primary model: `autoencoder_supervised`

The autoencoder is the selected model for future AI012 anomaly detection work. It had the strongest supervised anomaly-detection performance in the latest benchmark run.

Non-selected model experiments and the full 8-run comparison are archived in `other_models/`.
