# Other AI012 Models

Archived model experiments that were compared against the selected `autoencoder_supervised` run.

This folder keeps Isolation Forest, LOF, One-Class SVM, legacy M2 LOF assets, and the 8-run comparison outputs out of the main model path while preserving them for reference.
